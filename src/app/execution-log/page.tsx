import { Shell } from "@/components/dashboard"; import { ExecutionPage } from "@/components/data-pages"; import { getPageData } from "@/lib/dashboard-data"; export const dynamic="force-dynamic"; export default async function Page(){const d=await getPageData();return <Shell><ExecutionPage rows={d.runs}/></Shell>}
